PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sgso
# Poner DB_SSL=true cuando la base esta en la nube y exige conexion cifrada.
DB_SSL=false

# --- Autenticacion (login) ---
JWT_SECRET=cambiar_esta_clave_por_una_larga_y_secreta
JWT_EXPIRES_IN=8h

# --- Despliegue ---
# Dominios permitidos para llamar a la API (separados por coma). Vacio = cualquiera (solo dev).
# Ej: CORS_ORIGIN=https://mi-front.vercel.app
CORS_ORIGIN=
