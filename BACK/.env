PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sgso

# --- Autenticacion (login) ---
# Clave secreta para firmar los JWT. En produccion debe ser larga y secreta.
JWT_SECRET=cambiar_esta_clave_por_una_larga_y_secreta
# Tiempo de validez del token (formato de la libreria jsonwebtoken: 1h, 8h, 7d...)
JWT_EXPIRES_IN=8h
